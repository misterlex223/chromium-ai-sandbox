---
description: 產生產品規格範本文件
argument-hint: [output-path]
allowed-tools: ["Write"]
---

# /test-spec-template - 產生規格範本

產生一個空的產品規格範本文件，供前端開發者填寫。

## 用法

```
/test-spec-template [output-path]
```

## 參數

- `output-path`: 輸出檔案路徑 (選填)
  - 預設值: `spec-template.md`
  - 建議命名: `specs/[feature-name]-spec.md`

## 範例

```
/test-spec-template specs/login-spec.md
```

輸出：
```
✅ 規格範本已建立: specs/login-spec.md

💡 提示:
- 編輯該檔案填寫測試規格
- 完成後使用 /test-spec specs/login-spec.md 執行測試
```

## 範本內容

產生的檔案包含以下區塊：

1. **基本信息** - 功能名稱、版本、環境
2. **功能描述** - 功能用途說明
3. **用戶故事** - User Story 格式
4. **測試場景** - 可執行的測試案例
5. **UI/UX 驗收標準** - 視覺、互動、可訪問性
6. **錯誤處理** - 錯誤情境處理方式
7. **邊界條件** - 輸入限制、並發處理
8. **效能要求** - 載入時間、回應時間
